# Code Server

Basically an image of VS Code which can be used to modify the yaml files in Home Assistant configuration. 

# Docker Config Notes

Needs its only application path. Then needs a bind mount to the volume where home assistant config files are located and any other files that will need modification